[project]
name = "mani-work"
version = "0.1.0"
description = "Consists of all graphs and code for the data gathering and visualisation project."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.1",
    "selenium>=4.41.0",
    "setuptools>=82.0.1",
    "undetected-chromedriver>=3.5.5",
]

[tool.uv.workspace]
members = [
    "mani-work",
]
